## What and why

<!-- The problem this solves, then the approach. -->

## Checks

- [ ] `./gradlew ktlintFormat testDebugUnitTest lintDebug` passes locally
- [ ] New behaviour has a test where the layer allows it
- [ ] UI change → screenshot below
- [ ] Nothing here touches the backup format, or if it does, it is additive and the
      round-trip test proves it
